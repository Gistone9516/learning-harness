# 컴활1급 출제 함수 전체목록 (Excel 2021)

> 적용 출제기준: 2024.1.1 ~ 2026.12.31 (현행)  
> 기준 소프트웨어: Microsoft Office 2021 (Excel 2021)  
> 총 함수 수: **139개** (2급 포함 기준, 2024 개정 출제기준 반영)

---

## ★ 2024 출제기준 주요 변경사항

| 구분 | 내용 |
|------|------|
| **추가** | IFS, SWITCH, XLOOKUP, XMATCH |
| **삭제** | FINDB, SEARCHB, HYPERLINK, RANK.AVG |
| **명칭변경** | CONCATENATE → CONCAT, MODE → MODE.SNGL, STDEV → STDEV.S, VAR → VAR.S, PERCENTILE → PERCENTILE.INC |

---

## 1. 수학/삼각 함수 (14개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| SUM | `SUM(number1, [number2], ...)` | 인수의 합계 | ★★★★★ |
| SUMIF | `SUMIF(range, criteria, [sum_range])` | 조건에 맞는 셀 합계 | ★★★★★ |
| SUMIFS | `SUMIFS(sum_range, criteria_range1, criteria1, ...)` | 다중 조건 합계 | ★★★★★ |
| SUMPRODUCT | `SUMPRODUCT(array1, [array2], ...)` | 배열 요소 곱한 후 합산 (배열수식 대용) | ★★★★★ |
| ROUND | `ROUND(number, num_digits)` | 지정 자릿수로 반올림 | ★★★★☆ |
| ROUNDUP | `ROUNDUP(number, num_digits)` | 지정 자릿수로 올림 | ★★★★☆ |
| ROUNDDOWN | `ROUNDDOWN(number, num_digits)` | 지정 자릿수로 내림 | ★★★★☆ |
| INT | `INT(number)` | 소수점 이하 버리고 정수 반환 (음수: 내림) | ★★★☆☆ |
| TRUNC | `TRUNC(number, [num_digits])` | 지정 자릿수 이하 잘라냄 (0 방향) | ★★★☆☆ |
| MOD | `MOD(number, divisor)` | 나눗셈의 나머지 반환 | ★★★★☆ |
| QUOTIENT | `QUOTIENT(numerator, denominator)` | 나눗셈의 몫(정수 부분) 반환 | ★★★☆☆ |
| PRODUCT | `PRODUCT(number1, [number2], ...)` | 인수의 곱셈 | ★★☆☆☆ |
| SQRT | `SQRT(number)` | 양의 제곱근 반환 | ★★☆☆☆ |
| POWER | `POWER(number, power)` | 숫자의 거듭제곱 반환 | ★★☆☆☆ |
| ABS | `ABS(number)` | 절댓값 반환 | ★★★☆☆ |
| FACT | `FACT(number)` | 계승(팩토리얼) 반환 | ★★☆☆☆ |
| SIGN | `SIGN(number)` | 양수(1)/0(0)/음수(-1) 부호 반환 | ★★☆☆☆ |
| PI | `PI()` | 원주율(π) 반환 | ★☆☆☆☆ |
| FIXED | `FIXED(number, [decimals], [no_commas])` | 숫자를 텍스트로 천단위 형식화 | ★★☆☆☆ |
| RAND | `RAND()` | 0 이상 1 미만 난수 반환 | ★★☆☆☆ |
| RANDBETWEEN | `RANDBETWEEN(bottom, top)` | 지정 범위 사이 정수 난수 반환 | ★★☆☆☆ |

> **수학/삼각 소계: 21개**  
> ※ xlworks 기준 14개 분류이나 실제 출제 함수 기준 21개로 열거 (EXP, LOG 등 일부 포함)

---

## 2. 통계 함수 (24개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| AVERAGE | `AVERAGE(number1, [number2], ...)` | 인수의 평균 | ★★★★★ |
| AVERAGEA | `AVERAGEA(value1, [value2], ...)` | 텍스트/논리값 포함 평균 | ★★☆☆☆ |
| AVERAGEIF | `AVERAGEIF(range, criteria, [average_range])` | 조건에 맞는 셀 평균 | ★★★★☆ |
| AVERAGEIFS | `AVERAGEIFS(average_range, criteria_range1, criteria1, ...)` | 다중 조건 평균 | ★★★★☆ |
| COUNT | `COUNT(value1, [value2], ...)` | 숫자가 있는 셀 개수 | ★★★★★ |
| COUNTA | `COUNTA(value1, [value2], ...)` | 비어있지 않은 셀 개수 | ★★★★☆ |
| COUNTBLANK | `COUNTBLANK(range)` | 빈 셀 개수 | ★★★☆☆ |
| COUNTIF | `COUNTIF(range, criteria)` | 조건에 맞는 셀 개수 | ★★★★★ |
| COUNTIFS | `COUNTIFS(criteria_range1, criteria1, ...)` | 다중 조건 셀 개수 | ★★★★★ |
| MAX | `MAX(number1, [number2], ...)` | 최댓값 반환 | ★★★★★ |
| MAXA | `MAXA(value1, [value2], ...)` | 텍스트/논리값 포함 최댓값 | ★★☆☆☆ |
| MIN | `MIN(number1, [number2], ...)` | 최솟값 반환 | ★★★★★ |
| MINA | `MINA(value1, [value2], ...)` | 텍스트/논리값 포함 최솟값 | ★★☆☆☆ |
| MEDIAN | `MEDIAN(number1, [number2], ...)` | 중앙값 반환 | ★★★☆☆ |
| MODE.SNGL | `MODE.SNGL(number1, [number2], ...)` | 최빈값(가장 자주 나타나는 값) 반환 | ★★★☆☆ |
| LARGE | `LARGE(array, k)` | k번째로 큰 값 반환 | ★★★★★ |
| SMALL | `SMALL(array, k)` | k번째로 작은 값 반환 | ★★★★★ |
| RANK.EQ | `RANK.EQ(number, ref, [order])` | 지정 범위 내 순위 반환 (동순위 → 최고 순위) | ★★★★★ |
| FREQUENCY | `FREQUENCY(data_array, bins_array)` | 구간별 빈도수 반환 (배열수식) | ★★★★☆ |
| STDEV.S | `STDEV.S(number1, [number2], ...)` | 표본 표준편차 반환 | ★★★☆☆ |
| VAR.S | `VAR.S(number1, [number2], ...)` | 표본 분산 반환 | ★★★☆☆ |
| PERCENTILE.INC | `PERCENTILE.INC(array, k)` | k번째 백분위수 반환 (0~1 포함) | ★★★☆☆ |
| GEOMEAN | `GEOMEAN(number1, [number2], ...)` | 기하 평균 반환 | ★★☆☆☆ |
| HARMEAN | `HARMEAN(number1, [number2], ...)` | 조화 평균 반환 | ★★☆☆☆ |

> **통계 소계: 24개**

---

## 3. 논리 함수 (9개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| IF | `IF(logical_test, value_if_true, [value_if_false])` | 조건에 따라 다른 값 반환 | ★★★★★ |
| IFS | `IFS(logical_test1, value_if_true1, [logical_test2, value_if_true2], ...)` | 여러 조건 순서대로 검사 후 첫 번째 TRUE 결과 반환 (2024 추가) | ★★★★☆ |
| AND | `AND(logical1, [logical2], ...)` | 모든 인수가 TRUE이면 TRUE | ★★★★★ |
| OR | `OR(logical1, [logical2], ...)` | 인수 중 하나라도 TRUE이면 TRUE | ★★★★★ |
| NOT | `NOT(logical)` | 논리값 반전 | ★★★☆☆ |
| IFERROR | `IFERROR(value, value_if_error)` | 오류이면 대체값, 아니면 원래값 반환 | ★★★★★ |
| SWITCH | `SWITCH(expression, value1, result1, [value2, result2], ..., [default])` | 식을 값 목록과 비교해 첫 일치 결과 반환 (2024 추가) | ★★★☆☆ |
| TRUE | `TRUE()` | 논리값 TRUE 반환 | ★☆☆☆☆ |
| FALSE | `FALSE()` | 논리값 FALSE 반환 | ★☆☆☆☆ |

> **논리 소계: 9개**

---

## 4. 텍스트/문자열 함수 (20개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| LEFT | `LEFT(text, [num_chars])` | 왼쪽에서 지정 문자 수 추출 | ★★★★★ |
| RIGHT | `RIGHT(text, [num_chars])` | 오른쪽에서 지정 문자 수 추출 | ★★★★★ |
| MID | `MID(text, start_num, num_chars)` | 지정 위치부터 지정 문자 수 추출 | ★★★★★ |
| LEN | `LEN(text)` | 문자열 길이(문자 수) 반환 | ★★★★☆ |
| FIND | `FIND(find_text, within_text, [start_num])` | 텍스트 내 찾는 문자의 위치 반환 (대소문자 구분) | ★★★★☆ |
| SEARCH | `SEARCH(find_text, within_text, [start_num])` | 텍스트 내 찾는 문자 위치 반환 (대소문자 무시) | ★★★☆☆ |
| REPLACE | `REPLACE(old_text, start_num, num_chars, new_text)` | 지정 위치의 문자를 새 텍스트로 대체 | ★★★★☆ |
| SUBSTITUTE | `SUBSTITUTE(text, old_text, new_text, [instance_num])` | 지정 문자열을 새 문자열로 대체 | ★★★★☆ |
| CONCAT | `CONCAT(text1, [text2], ...)` | 여러 텍스트 연결 (CONCATENATE 대체) | ★★★★☆ |
| TEXT | `TEXT(value, format_text)` | 숫자를 지정 형식의 텍스트로 변환 | ★★★★★ |
| VALUE | `VALUE(text)` | 숫자를 나타내는 텍스트를 숫자로 변환 | ★★★☆☆ |
| UPPER | `UPPER(text)` | 텍스트를 모두 대문자로 변환 | ★★★☆☆ |
| LOWER | `LOWER(text)` | 텍스트를 모두 소문자로 변환 | ★★★☆☆ |
| PROPER | `PROPER(text)` | 각 단어 첫 글자만 대문자로 변환 | ★★★☆☆ |
| REPT | `REPT(text, number_times)` | 텍스트를 지정 횟수만큼 반복 | ★★★☆☆ |
| TRIM | `TRIM(text)` | 앞뒤 공백 및 단어 사이 이중 공백 제거 | ★★★☆☆ |
| CLEAN | `CLEAN(text)` | 인쇄 불가 문자 제거 | ★★☆☆☆ |
| EXACT | `EXACT(text1, text2)` | 두 텍스트가 완전히 같으면 TRUE (대소문자 구분) | ★★☆☆☆ |
| INDIRECT | `INDIRECT(ref_text, [a1])` | 텍스트로 표현된 참조를 실제 셀 참조로 변환 | ★★★★★ |
| DATEVALUE | `DATEVALUE(date_text)` | 텍스트 형식 날짜를 일련번호(날짜값)로 변환 | ★★★☆☆ |

> **텍스트/문자열 소계: 20개**  
> ※ INDIRECT, DATEVALUE는 일부 교재에서 참조/날짜 분류이나 xlworks 1급 함수 목록에서 텍스트 계열로 분류

---

## 5. 날짜/시간 함수 (20개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| TODAY | `TODAY()` | 오늘 날짜 반환 | ★★★★★ |
| NOW | `NOW()` | 현재 날짜와 시간 반환 | ★★★★☆ |
| DATE | `DATE(year, month, day)` | 연/월/일로 날짜값 생성 | ★★★★★ |
| TIME | `TIME(hour, minute, second)` | 시/분/초로 시간값 생성 | ★★★☆☆ |
| YEAR | `YEAR(serial_number)` | 날짜에서 연도 추출 | ★★★★★ |
| MONTH | `MONTH(serial_number)` | 날짜에서 월 추출 | ★★★★★ |
| DAY | `DAY(serial_number)` | 날짜에서 일 추출 | ★★★★★ |
| HOUR | `HOUR(serial_number)` | 시간에서 시 추출 | ★★★☆☆ |
| MINUTE | `MINUTE(serial_number)` | 시간에서 분 추출 | ★★★☆☆ |
| SECOND | `SECOND(serial_number)` | 시간에서 초 추출 | ★★☆☆☆ |
| WEEKDAY | `WEEKDAY(serial_number, [return_type])` | 날짜의 요일 번호 반환 (1=일 ~ 7=토 기본) | ★★★★☆ |
| WEEKNUM | `WEEKNUM(serial_number, [return_type])` | 날짜가 해당 연도의 몇 번째 주인지 반환 | ★★★☆☆ |
| ISOWEEKNUM | `ISOWEEKNUM(date)` | ISO 기준 주 번호 반환 | ★★☆☆☆ |
| EDATE | `EDATE(start_date, months)` | 시작일에서 월수 더하거나 뺀 날짜 반환 | ★★★★☆ |
| EOMONTH | `EOMONTH(start_date, months)` | 월수 이후 해당 월의 마지막 날 반환 | ★★★★☆ |
| DAYS | `DAYS(end_date, start_date)` | 두 날짜 사이의 일수 반환 | ★★★☆☆ |
| DAYS360 | `DAYS360(start_date, end_date, [method])` | 1년=360일 기준 날짜 차이 반환 | ★★☆☆☆ |
| YEARFRAC | `YEARFRAC(start_date, end_date, [basis])` | 두 날짜 사이의 연 비율 반환 | ★★☆☆☆ |
| WORKDAY | `WORKDAY(start_date, days, [holidays])` | 영업일 기준 완료 날짜 반환 | ★★★★☆ |
| WORKDAY.INTL | `WORKDAY.INTL(start_date, days, [weekend], [holidays])` | 주말 유형 지정 가능한 영업일 완료 날짜 반환 | ★★★☆☆ |
| NETWORKDAYS | `NETWORKDAYS(start_date, end_date, [holidays])` | 두 날짜 사이의 영업일 수 반환 | ★★★★☆ |
| NETWORKDAYS.INTL | `NETWORKDAYS.INTL(start_date, end_date, [weekend], [holidays])` | 주말 유형 지정 가능한 영업일 수 반환 | ★★★☆☆ |

> **날짜/시간 소계: 22개** (DATEVALUE 포함 시 23개, 본 문서에서는 텍스트 분류로 처리)

---

## 6. 찾기/참조 함수 (17개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| VLOOKUP | `VLOOKUP(lookup_value, table_array, col_index_num, [range_lookup])` | 열 기준 세로 방향 검색 후 지정 열 값 반환 | ★★★★★ |
| HLOOKUP | `HLOOKUP(lookup_value, table_array, row_index_num, [range_lookup])` | 행 기준 가로 방향 검색 후 지정 행 값 반환 | ★★★★☆ |
| LOOKUP | `LOOKUP(lookup_value, lookup_vector, [result_vector])` | 행/열 1차원 범위에서 값 검색 | ★★★☆☆ |
| XLOOKUP | `XLOOKUP(lookup_value, lookup_array, return_array, [if_not_found], [match_mode], [search_mode])` | 범위/배열 검색, 일치값 반환 (VLOOKUP 진화형, 2024 추가) | ★★★★☆ |
| INDEX | `INDEX(array, row_num, [col_num])` | 지정 행·열 교차 위치의 값 반환 | ★★★★★ |
| MATCH | `MATCH(lookup_value, lookup_array, [match_type])` | 범위에서 값의 상대적 위치(순번) 반환 | ★★★★★ |
| XMATCH | `XMATCH(lookup_value, lookup_array, [match_mode], [search_mode])` | MATCH 진화형, 역방향·와일드카드 지원 (2024 추가) | ★★★☆☆ |
| CHOOSE | `CHOOSE(index_num, value1, [value2], ...)` | 인덱스 번호에 해당하는 값 반환 | ★★★★★ |
| OFFSET | `OFFSET(reference, rows, cols, [height], [width])` | 기준 셀에서 행·열 이동 후 참조 반환 | ★★★★★ |
| ADDRESS | `ADDRESS(row_num, column_num, [abs_num], [a1], [sheet_text])` | 행·열 번호로 셀 주소 텍스트 반환 | ★★★☆☆ |
| ROW | `ROW([reference])` | 참조의 행 번호 반환 | ★★★★☆ |
| COLUMN | `COLUMN([reference])` | 참조의 열 번호 반환 | ★★★☆☆ |
| ROWS | `ROWS(array)` | 배열/범위의 행 수 반환 | ★★☆☆☆ |
| COLUMNS | `COLUMNS(array)` | 배열/범위의 열 수 반환 | ★★☆☆☆ |
| AREAS | `AREAS(reference)` | 참조에 포함된 영역 수 반환 | ★★☆☆☆ |
| TRANSPOSE | `TRANSPOSE(array)` | 배열의 행과 열을 바꿔 반환 (배열수식) | ★★★☆☆ |
| INDIRECT | `INDIRECT(ref_text, [a1])` | 텍스트 문자열로 표현된 참조를 실제 참조로 변환 | ★★★★★ |

> **찾기/참조 소계: 17개**  
> ※ INDIRECT는 텍스트 함수와 중복 분류됨 — 본 문서에서는 찾기/참조 분류 기준

---

## 7. 데이터베이스 함수 (10개)

> 공통 문법 패턴: `D함수(database, field, criteria)`  
> - database: 데이터베이스 전체 범위(헤더 포함)  
> - field: 집계할 열(헤더명 또는 열 번호)  
> - criteria: 조건 범위(헤더+조건값)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| DSUM | `DSUM(database, field, criteria)` | 조건에 맞는 필드 값의 합계 | ★★★★★ |
| DAVERAGE | `DAVERAGE(database, field, criteria)` | 조건에 맞는 필드 값의 평균 | ★★★★★ |
| DCOUNT | `DCOUNT(database, field, criteria)` | 조건에 맞는 숫자 셀 개수 | ★★★★☆ |
| DCOUNTA | `DCOUNTA(database, field, criteria)` | 조건에 맞는 비어있지 않은 셀 개수 | ★★★☆☆ |
| DMAX | `DMAX(database, field, criteria)` | 조건에 맞는 필드 값의 최댓값 | ★★★★☆ |
| DMIN | `DMIN(database, field, criteria)` | 조건에 맞는 필드 값의 최솟값 | ★★★★☆ |
| DGET | `DGET(database, field, criteria)` | 조건에 맞는 단 하나의 값 반환 | ★★★★☆ |
| DPRODUCT | `DPRODUCT(database, field, criteria)` | 조건에 맞는 필드 값의 곱 | ★★☆☆☆ |
| DVAR | `DVAR(database, field, criteria)` | 조건에 맞는 필드 값의 표본 분산 | ★★☆☆☆ |
| DSTDEV | `DSTDEV(database, field, criteria)` | 조건에 맞는 필드 값의 표본 표준편차 | ★★☆☆☆ |

> **데이터베이스 소계: 10개**

---

## 8. 재무 함수 (6개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| PMT | `PMT(rate, nper, pv, [fv], [type])` | 정기 대출 상환액 계산 | ★★★★★ |
| PV | `PV(rate, nper, pmt, [fv], [type])` | 투자의 현재 가치 계산 | ★★★★☆ |
| FV | `FV(rate, nper, pmt, [pv], [type])` | 투자의 미래 가치 계산 | ★★★★☆ |
| NPV | `NPV(rate, value1, [value2], ...)` | 순현재가치 계산 | ★★★☆☆ |
| SLN | `SLN(cost, salvage, life)` | 정액법(직선법) 감가상각액 계산 | ★★★☆☆ |
| DDB | `DDB(cost, salvage, life, period, [factor])` | 이중체감법 감가상각액 계산 | ★★★☆☆ |
| SYD | `SYD(cost, salvage, life, per)` | 연수합계법 감가상각액 계산 | ★★☆☆☆ |
| DB | `DB(cost, salvage, life, period, [month])` | 정률법 감가상각액 계산 | ★★☆☆☆ |
| AMORLINC | `AMORLINC(cost, date_purchased, first_period, salvage, period, rate, [basis])` | 각 회계 기간의 감가상각액 반환 (선형) | ★☆☆☆☆ |
| AMORDEGRC | `AMORDEGRC(cost, date_purchased, first_period, salvage, period, rate, [basis])` | 각 회계 기간의 감가상각액 반환 (체감) | ★☆☆☆☆ |

> **재무 소계: 10개**

---

## 9. 정보 함수 (16개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| ISBLANK | `ISBLANK(value)` | 빈 셀이면 TRUE | ★★★★☆ |
| ISERROR | `ISERROR(value)` | 오류값이면 TRUE (#N/A 포함) | ★★★★☆ |
| ISERR | `ISERR(value)` | #N/A 제외 오류값이면 TRUE | ★★★☆☆ |
| ISNA | `ISNA(value)` | #N/A 오류이면 TRUE | ★★★☆☆ |
| ISNUMBER | `ISNUMBER(value)` | 숫자이면 TRUE | ★★★★☆ |
| ISTEXT | `ISTEXT(value)` | 텍스트이면 TRUE | ★★★☆☆ |
| ISNONTEXT | `ISNONTEXT(value)` | 텍스트가 아니면 TRUE (빈 셀 포함) | ★★☆☆☆ |
| ISLOGICAL | `ISLOGICAL(value)` | 논리값이면 TRUE | ★★☆☆☆ |
| ISODD | `ISODD(value)` | 홀수이면 TRUE | ★★★☆☆ |
| ISEVEN | `ISEVEN(value)` | 짝수이면 TRUE | ★★★☆☆ |
| ISFORMULA | `ISFORMULA(reference)` | 수식이 있는 셀이면 TRUE | ★★☆☆☆ |
| ISREF | `ISREF(value)` | 참조이면 TRUE | ★★☆☆☆ |
| CELL | `CELL(info_type, [reference])` | 셀의 서식/위치/내용 정보 반환 | ★★★☆☆ |
| TYPE | `TYPE(value)` | 값의 데이터 형식 번호 반환 | ★★☆☆☆ |
| N | `N(value)` | 값을 숫자로 변환 | ★★☆☆☆ |
| NA | `NA()` | 오류값 #N/A 반환 | ★★☆☆☆ |

> **정보 소계: 16개**

---

## 10. 행렬 함수 (3개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| MMULT | `MMULT(array1, array2)` | 두 배열의 행렬 곱 반환 (배열수식) | ★★☆☆☆ |
| MINVERSE | `MINVERSE(array)` | 정방행렬의 역행렬 반환 (배열수식) | ★★☆☆☆ |
| MDETERM | `MDETERM(array)` | 정방행렬의 행렬식(행렬식 값) 반환 | ★★☆☆☆ |

> **행렬 소계: 3개**

---

## 11. 기타/집계 함수 (4개)

| 함수명 | 문법 | 용도/한줄설명 | 1급 빈출도 |
|--------|------|---------------|-----------|
| SUBTOTAL | `SUBTOTAL(function_num, ref1, ...)` | 목록/필터된 데이터 부분합 계산 | ★★★★☆ |
| EXP | `EXP(number)` | e의 거듭제곱 반환 | ★★☆☆☆ |
| LOG | `LOG(number, [base])` | 지정 밑수의 로그 반환 | ★★☆☆☆ |
| LOG10 | `LOG10(number)` | 상용로그(밑 10) 반환 | ★☆☆☆☆ |

> **기타 소계: 4개**

---

## ★★ 1급 고난도 핵심 함수 강조 섹션

> 배열수식, 중첩함수, 동적 참조 등 1급 실기 고난도 출제 핵심

### A. 배열수식 필수 함수
| 함수명 | 핵심 활용 패턴 | 비고 |
|--------|--------------|------|
| SUMPRODUCT | `=SUMPRODUCT((조건1)*(조건2)*값범위)` — 다중조건 합산 | 배열수식 없이 사용 가능 |
| FREQUENCY | `{=FREQUENCY(data,bins)}` — Ctrl+Shift+Enter | 반드시 배열수식 |
| TRANSPOSE | `{=TRANSPOSE(array)}` — 행/열 전환 | 반드시 배열수식 |
| MMULT | `{=MMULT(array1,array2)}` — 행렬 곱 | 반드시 배열수식 |

### B. 동적 참조/중첩 핵심 조합
| 조합 패턴 | 활용 예 |
|-----------|--------|
| INDEX + MATCH | `=INDEX(반환범위, MATCH(찾을값, 찾기범위, 0))` — VLOOKUP 한계 극복 |
| OFFSET + MATCH | `=OFFSET(기준, MATCH(값,범위,0)-1, 열이동)` — 동적 범위 참조 |
| INDIRECT + ADDRESS | `=INDIRECT(ADDRESS(행번호, 열번호))` — 동적 셀 주소 참조 |
| CHOOSE + 수식 | `=CHOOSE(INT((MONTH(날짜)-1)/3)+1,"1분기","2분기","3분기","4분기")` |
| 중첩 IF / IFS | `=IFS(조건1,값1,조건2,값2,TRUE,기타)` |

### C. 조건부 집계 핵심
| 함수 | 단일조건 | 다중조건 |
|------|---------|---------|
| 합계 | SUMIF | SUMIFS |
| 평균 | AVERAGEIF | AVERAGEIFS |
| 개수 | COUNTIF | COUNTIFS |
| DB 조건 | DSUM, DAVERAGE, DCOUNT, DMAX, DMIN, DGET | — |

### D. 찾기/참조 핵심 비교
| 함수 | 방향 | 정확일치 기본 | 역방향 | 2024 신규 |
|------|------|------------|--------|---------|
| VLOOKUP | 세로(열) | FALSE 지정 필요 | X | — |
| HLOOKUP | 가로(행) | FALSE 지정 필요 | X | — |
| XLOOKUP | 자유 | 기본값 정확 | O | ✅ |
| INDEX+MATCH | 자유 | match_type=0 | 가능 | — |
| XMATCH | 위치 반환 | 기본값 정확 | O | ✅ |

### E. 재무 함수 인수 공통 구조
```
rate  = 기간당 이자율 (연이율이면 /12 적용)
nper  = 총 납입 기간 수
pmt   = 정기 납입액 (납입 = 음수)
pv    = 현재 가치 (대출 원금 = 양수)
fv    = 미래 가치 (기본값 0)
type  = 납입 시점 (0=기말, 1=기초)
```

---

## 카테고리별 개수 집계

| 카테고리 | 함수 수 |
|---------|--------|
| 수학/삼각 | 21 |
| 통계 | 24 |
| 논리 | 9 |
| 텍스트/문자열 | 20 |
| 날짜/시간 | 22 |
| 찾기/참조 | 17 |
| 데이터베이스 | 10 |
| 재무 | 10 |
| 정보 | 16 |
| 행렬 | 3 |
| 기타/집계 | 4 |
| **합계** | **156** |

> **실제 열거 함수 수: 156개**  
> ※ 공식 출제기준 표기 "139개"는 2018~2020 기준. 2024 개정(IFS·SWITCH·XLOOKUP·XMATCH 추가, 일부 삭제)으로 실질 범위 확대. INDIRECT는 텍스트·참조 중복 분류 발생 — 본 문서에서는 찾기/참조 단일 계상.

---

## 출처

| # | 출처 | URL |
|---|------|-----|
| 1 | 엑셀웍스 — 컴활 자격시험 함수목록 (공신력 ★★★★★) | https://xlworks.net/comcert_list/ |
| 2 | 링커리어 — 2024 컴활 개편사항 | https://community.linkareer.com/jayuu/2413845 |
| 3 | 링커리어 — 2024 컴활 1급 함수 총정리 | https://community.linkareer.com/best/2565080 |
| 4 | Heoni Tech — 컴활 1급 실기 자주 나오는 함수 | https://heoni00.github.io/cumhwal/cumhwal-02/ |
| 5 | kimsungtae.co.kr — 2025 컴활 1급 실기 핵심 함수 30개 | https://www.kimsungtae.co.kr/2025/05/blog-post_12.html |
| 6 | 대한상공회의소 — 종목소개/시험안내 (공식) | https://license.korcham.net/co/examguide.do?mm=21&cd=0103 |
| 7 | Microsoft Support — Excel 함수 참조 (공식 문법) | https://support.microsoft.com/en-us/office/excel-functions-alphabetical-b3944572-255d-4efb-bb96-c6d90033e188 |
| 8 | 엑셀웍스 — DB 함수 개별 페이지 | https://xlworks.net/excel-function-dsum/ |

---

## 조사 메타

| 항목 | 내용 |
|------|------|
| 조사일 | 2026-06-06 |
| 기준 출제기준 | 2024.1.1 ~ 2026.12.31 (현행) |
| 기준 소프트웨어 | Excel 2021 (Office 2021) |
| 공식 함수 수 | 139개 (2018-2020 기준 xlworks 표기) |
| 본 문서 열거 수 | **156개** (2024 개정 추가 반영, INDIRECT 중복 단일 계상) |
| 수집 방법 | WebSearch 5회 병렬 + WebFetch 6개 소스 |
| **coverage 자가보고** | 수학/삼각·통계·논리·텍스트·날짜/시간·찾기/참조·DB·재무·정보·행렬 카테고리 커버. **불확실/누락 가능 영역**: ① EXP/LOG/LOG10 등 수학 고급 함수 일부 누락 가능 ② 2024 개정 기준 정확한 총 함수 수 미확인 (대한상공회의소 공식 문서 직접 접근 불가) ③ AMORLINC/AMORDEGRC는 구 기준 포함이며 현행 기준 유지 여부 불확실 ④ SUBTOTAL은 일부 교재에서 1급 범위로 다루나 공식 목록 미확인 |
| 갭 | 대한상공회의소 공식 출제기준 PDF 직접 열람 불가 (요약/간접 확인). 2024 개정 후 정확한 함수 총수 공식 확인 필요 시 license.korcham.net 직접 확인 권장 |
